public class App {
    public static void main(String[] args){
        /*UserDAO userDAO = new UserDAO();
        User testUser = new User(1, "testing", "testing", "testing@abv.bg");
        try {
            userDAO.add(testUser);
        } catch (SQLException exception) {
            exception.printStackTrace();
        }
        UserDAO userDAO1 = null;
        try {
            userDAO1 = new UserDAO(testUser.getUsername(), testUser.getPassword());
        } catch (SQLException exception) {
            exception.printStackTrace();
        }

        System.out.println(userDAO1.getUser());*/
    }
}
